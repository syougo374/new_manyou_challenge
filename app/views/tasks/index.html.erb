<h1>タスク一覧</h1>
<table>
  <% @tasks.each do |form| %>
    <tr>
      <td><%= form.title %></td>
      <td><%=form.content %></td>
    </tr>
  <% end %>
</table>
<%= link_to 'タスクを追加する', new_task_path %>
